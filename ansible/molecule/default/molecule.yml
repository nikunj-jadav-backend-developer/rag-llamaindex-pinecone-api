---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: ubuntu-instance
    image: geerlingguy/docker-ubuntu2204-ansible
    privileged: true
    pre_build_image: true

provisioner:
  name: ansible

verifier:
  name: ansible